specVersion: "v1beta"
serviceId: "student-management"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "student-management-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
